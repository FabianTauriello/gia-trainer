function QuestionModal({ show, onClose }: { show: boolean; onClose: () => void }) {
  return (
    // <Modal dismissible show={show}>
    <div>some content</div>
    // </Modal>
  );
}

export default QuestionModal;
